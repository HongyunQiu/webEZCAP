// 本地 stdint_windows.h，使用 C++ 标准库而不是 SDK 的 C 语言版本
#ifndef _STDINT_WINDOWS_H
#define _STDINT_WINDOWS_H 1

// 包含 C++ 标准库的 cstdint
#include <cstdint>

// 在 MSVC 中，<cstdint> 的类型直接在全局命名空间中
// 不需要额外的定义

#endif // _STDINT_WINDOWS_H

